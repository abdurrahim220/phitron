#include <bits/stdc++.h>
using namespace std;

// void swap(int &a, int &b)
// {
//     int temp = a;
//     a = b;
//     b = temp;
// }
int main()
{

    int a, b;
    cin >> a >> b;
    swap(a, b);
    cout << "After swapping: " << a << " " << b << endl;
    // int a, b, c, d;
    // cin >> a >> b >> c >> d;

    // int c = min(a, b);
    // int d = max(a, b);
    // int mn= min({a, b, c, d});
    // int mx= max({a, b, c, d});
    // cout << "Minimum: " << c << endl;
    // cout << "Maximum: " << d << endl;
    // cout << "Minimum: " << mn << endl;
    // cout << "Maximum: " << mx << endl;

    return 0;
}