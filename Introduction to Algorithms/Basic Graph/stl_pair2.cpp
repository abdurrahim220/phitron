#include <bits/stdc++.h>
using namespace std;
int main()
{
    // pair<int, int> p;
    pair<string, int> p;
    // p = make_pair(10, 20);
    p = make_pair("rahim", 20);
    cout << p.first << " " << p.second << endl;
    return 0;
}